# Localhost group - Jenkins runs ansible playbooks locally
[local]
localhost ansible_connection=local

# Example remote group for testing launched instances (e.g., EC2)
[tstlaunched]
# ec2-user@your-ec2-ip ansible_ssh_private_key_file=~/.ssh/psp_ansible_key ansible_user=ec2-user
